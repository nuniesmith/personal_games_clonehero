apiVersion: 1
providers:
  - name: Default
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30  # Refresh interval
    options:
      path: /etc/grafana/provisioning/dashboards
